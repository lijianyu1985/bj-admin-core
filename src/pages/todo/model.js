import modelFactory from '../../utils/modelFactory';

const Model = modelFactory({
  namespace: 'todo',
  state: {
    list: [],
    resources: [],
  },
  effects: {
  },
  reducers: {
  },
});
export default Model;
